[project]
name = "declare"
version = "0.1.0"
description = "Open-source framework for doing data engineering & analytics development faster, built on Clickhouse"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hatchet-sdk>=0.38.1",
    "python-dotenv>=1.0.1",
]
